import { Panel } from "../components/Panel.ripple";
import { Link } from 'ripple-router';

export component CreateContextPage() {
    <div class="flex-1 flex flex-col justify-center items-center text-center p-4">
        <Panel color="bg-[#ff5733]" class="w-content h-content max-h-9/10 max-w-[1200px] bg-[#1f2028]/50 backdrop-blur-[5px] border-[#ff5733]/20 border-2 flex content-start flex-col text-start">
            <div class="w-full h-full overflow-y-auto flex flex-col p-8 gap-2">
                <h1 class="text-4xl font-bold">{"Nieuwe context aanmaken"}</h1>

                <div> 
                    <label for="contextType" class="text-white/50 mb-2">{"Selecteer type context"}</label>
                    <select name="contextType" class="mb-4 p-2 bg-[#2a2b33] border-2 border-white/10 text-white w-full max-w-[600px]">
                        <option value="" disabled selected> {"Anders"} </option>
                        <option value="vak"> {"Vak"} </option>
                        <option value="project"> {"Project"} </option>
                    </select>
                </div>

                <div> 
                    <label for="contextType" class="text-white/50 mb-2">{"Context naam"}</label>
                    <input type="text" name="contextName" placeholder="bijv. Object Oriented Software Development en Design" class="mb-4 p-2 bg-[#2a2b33] border-2 border-white/10 text-white w-full max-w-[600px]" />
                </div>

                <div> 
                    <label for="contextType" class="text-white/50 mb-2">{"Context voor LLM"}</label>
                    <textarea
                        type="textbox" name="contextName" 
                        placeholder="Beschrijf wat studenten moeten leren binnen deze context. De AI gebruikt dit om gerichte hulp te geven." 
                        class="mb-4 p-2 bg-[#2a2b33] border-2 border-white/10 text-white w-full min-h-[150px] max-w-[600px]" 
                    />
                </div>

                <div class="flex flex-col md:flex-row gap-4">
                    <span class="text-white/50 mt-4">
                        <Link href="/control" class="px-4 py-2 bg-[#ff5733] text-white hover:bg-[#e04e2a] cursor-pointer">
                            {"Context aanmaken"}
                        </Link>
                    </span>
                    <span class="text-white/50 mt-4">
                        <Link href="/control" class="px-4 py-2 bg-[#2a2b33] text-white hover:bg-[#e04e2a] cursor-pointer">
                            {"Annuleren"}
                        </Link>
                    </span>
                </div>

            </div>
                
        </Panel>
    </div>
}